[project]
name = "ai-agent"
version = "0.1.0"
description = "CLI chatbot using Google Gemini with function calling for file operations and Python execution"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
